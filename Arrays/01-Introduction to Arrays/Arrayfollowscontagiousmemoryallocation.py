# Declare and initialize a list of integers
arr = [1, 2, 3, 4, 5]

# Print the memory address of the first element
print(f"Address of first element: {id(arr[0])}")

# Print the memory address of the second element
print(f"Address of second element: {id(arr[1])}")

# Print the memory address of the third element
print(f"Address of third element: {id(arr[2])}")
