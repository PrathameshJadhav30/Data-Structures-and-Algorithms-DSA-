# Declare and initialize a list of integers
arr = [1, 2, 3, 4, 5]

# Calculate the number of elements in the list
arr_len = len(arr)

# Loop through the list to access and display each element
for i in range(arr_len):
    print(f"Element {i} = {arr[i]}")
